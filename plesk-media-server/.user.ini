; POI•LOVE — media.poilove.com — PHP-FPM overrides
; Questo file viene letto da PHP-FPM (FastCGI) — non da mod_php
upload_max_filesize = 10M
post_max_size = 12M
max_execution_time = 60
memory_limit = 128M
