# Dipendenze
node_modules/
.pnp
.pnp.js

# Build Expo
.expo/
dist/
web-build/
ios/build/
android/build/
android/.gradle/

# Variabili d'ambiente — MAI committare
.env
.env.local
.env.*.local
*.env

# Chiavi API — solo sul server, mai nel repo
webapp/config.js
config.js

# Documenti strategici e sensibili — MAI nel repo pubblico (business plan + analisi debolezze)
SPECS.md
POSTMORTEM.md

# Cartella commerciale riservata (listini, modello di business, piano marketing): il repo e PUBBLICO
*DOSSIER*/
# materiale commerciale riservato: il repo e' pubblico, questa cartella no
_marketing/

# Cache e log
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db

# Sorgenti Photoshop: fuori dal repo (vanno in magazzino)
*.psd
*.PSD

# Supabase CLI: file locali temporanei (non committare)
supabase/.temp/

# Chiavi client ricerca immagini (mai su repo pubblico)
admin/js/media-keys.js

# Piano trasloco server — dettagli infrastruttura, mai nel repo pubblico
TRASLOCO-ORDINI.md
RIFINITURE-MACCHINA-NUOVA.md

# ── Foto caricate dagli utenti (POI, avatar, copertine): MAI su GitHub ──
# Vivono sulla macchina e su Supabase Storage, e sono protette dal backup della macchina.
# Il repository e' pubblico e tiene solo gli asset del progetto (loghi, mascotte, opengraph).
plesk-media-server/poi/
plesk-media-server/uploads/
media/poi/
**/media/poi/
**/uploads/
*.heic
*.HEIC
PIANO-ACCESSI-INDIPENDENTI.md
RITORNO-INDIETRO.md

# Copie di sicurezza: MAI su GitHub, questo repository e' pubblico.
# Contengono i dati degli utenti e le fotografie.
backup/
*.dump
*.tar.gz
